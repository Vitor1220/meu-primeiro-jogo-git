programa {
  funcao inicio() {
  cadeia letra 
  escreva("digite uma letra:")
  leia(letra)  
  se(letra == "a" ou letra == "e" ou letra == "i" ou letra == "o" ou letra == "u" ou letra == "A" ou letra == "E" ou letra == "I" ou letra == "O" ou letra ou letra == "U"){
    escreva("esta letra é uma vogal!")
  } senao{
    escreva("essa letra é uma consoante.") 
  }


  }
}
